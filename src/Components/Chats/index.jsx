import React from 'react';

const Chats = () => {
    // const test = useContext(AuthContext)
    // console.log('from chat component',test)
    return (
        <>
            <h1>this is Chats component</h1>
        </>
    );
}
export default React.memo(Chats);